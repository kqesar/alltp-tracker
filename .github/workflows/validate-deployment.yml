name: Validate Deployment PR

on:
  pull_request:
    branches:
      - gh-pages
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'deploy/')
    
    steps:
      - name: Checkout source branch (master)
        uses: actions/checkout@v6
        with:
          # Get the source commit from the PR body
          ref: master

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '10'

      - name: Install dependencies
        run: pnpm install

      - name: Run linter
        run: pnpm lint

      - name: Run tests
        run: pnpm test

      - name: Build project
        run: pnpm build

      - name: Verify build artifacts
        run: |
          if [ ! -d "dist" ]; then
            echo "❌ Build failed: dist directory not found"
            exit 1
          fi
          
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Build failed: index.html not found in dist"
            exit 1
          fi
          
          echo "✅ Build verification successful"

      - name: Comment PR status
        uses: actions/github-script@v8
        with:
          script: |
            const comment = `## ✅ Deployment Validation Successful

            All checks have passed:
            - ✅ **Linting**: Code style validation passed
            - ✅ **Tests**: All unit tests passed  
            - ✅ **Build**: Production build successful
            - ✅ **Artifacts**: Build artifacts verified

            This deployment PR is ready to be merged! 🚀

            ---
            *Automated validation completed at ${new Date().toISOString()}*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
